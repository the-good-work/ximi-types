import {
  Room,
  Participant,
  AudioPreset,
  LayoutPreset,
  Slot,
  VideoLayout,
} from "./room";

export namespace XIMI {
  export { Room, Participant, AudioPreset, LayoutPreset, Slot, VideoLayout };
}
